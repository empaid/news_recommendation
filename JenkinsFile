pipeline {
    agent any
    
    stages {
        
        stage('Code Analysis') {
            steps {
                script {
                    sh "sudo chown root:jenkins /run/docker.sock"
                    sh 'sudo docker-compose up'
                }
            }
        }
    }
}