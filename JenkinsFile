pipeline {
    agent any
    
    stages {
        
        stage('Code Analysis') {
            steps {
                script {
                    sh "docker rm -v -f $(docker ps -qa)"
                    sh "sudo chown root:jenkins /run/docker.sock"
                    sh 'sudo docker compose up'
                }
            }
        }
    }
}